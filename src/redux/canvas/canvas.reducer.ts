


const initalState = {
    width: null,
    height: null,
    background: null,
    maxWidth: null,
    maxHeight: null, 
}

const canvasReducer = (state=initalState, action:any) => {
    switch(action.type){
        default:
            return state;
    }
}

export default canvasReducer;